# Service Worker Documentation (sw.js)

## Overview

The service worker (`sw.js`) acts as the central communication hub and orchestration layer for the Om_E_Web Chrome Extension. It bridges the WebSocket server, content scripts, and Chrome APIs to enable external control of web pages.

**Core Responsibilities:**
- Maintain WebSocket connection to server (port 17892)
- Route commands between server and content scripts
- Manage tab lifecycle and state tracking
- Coordinate DOM scanning operations
- Handle capability execution (smart routing)
- Manage extension keep-alive mechanisms
- Track site configurations for per-domain behavior

---

## Architecture Overview

### Communication Flow

```
┌─────────────────┐
│  WebSocket      │
│  Server         │
│  (port 17892)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌──────────────┐      ┌─────────────┐
│  Service Worker │─────▶│   Content    │─────▶│     DOM     │
│     (sw.js)     │◀─────│   Script     │◀─────│  (Web Page) │
└─────────────────┘      └──────────────┘      └─────────────┘
         │
         ▼
┌─────────────────┐
│  Chrome APIs    │
│  (tabs, etc)    │
└─────────────────┘
```

### Message Flow Round-Trip

1. **Server → SW**: WebSocket receives command
2. **SW → Content**: chrome.tabs.sendMessage forwards to content script
3. **Content → DOM**: Content script executes on page
4. **Content → SW**: sendResponse returns result
5. **SW → Server**: WebSocket sends response back
6. **Server → Client**: Routes to original test client

---

## Global State Management

### State Variables

```javascript
// WebSocket connection
let ws = null;
let isConnected = false;
let pendingMessages = [];

// Tab state tracking
const tabState = new Map();           // tabId -> { scanInProgress, lastUrl, isRefresh }
const tabScanState = new Map();       // DEPRECATED - legacy compatibility
let internalTabState = new Map();     // tabId -> enhanced tab info
let lastActiveTabId = null;
let tabCache = new Map();             // tabId -> cached data

// Site configurations
let siteConfigs = {};                 // domain -> config object

// Keep-alive system
const keepAlivePorts = new Set();
let lastHeartbeatSent = 0;

// Action coordination (NOTE: actionInProgress used but not declared - potential bug)

// Orb UI state - persists across page navigations
const orbState = {
    theme: 'robot',       // Current theme name (robot is default)
    position: null,       // { left: number, top: number } or null for default
    chatVisible: false,   // Chat panel open/closed
    chatInput: '',        // Text in input box (persists across nav)
    chatPanelSize: null   // Chat panel dimensions { width, height } or null for default
};
```

### Tab State Structure (`internalTabState`)

```javascript
{
  id: number,
  url: string,
  title: string,
  active: boolean,
  status: string,
  lastUpdate: timestamp,
  needsFreshScan: boolean,
  contentScriptFresh: boolean,
  cacheCleared: boolean,

  // DOM change tracking
  domChanges: {
    totalChanges: number,
    lastChangeTime: timestamp,
    changeTypes: Set<string>,
    lastMutationCount: number
  },

  // Site config tracking
  siteConfigSent: boolean,
  lastConfigSent: timestamp,
  currentDomain: string,
  currentFramework: string,
  siteConfigError: string,
  lastConfigError: timestamp,

  // Network activity
  networkActivity: {
    inflightRequests: number,
    lastActivity: timestamp,
    recentRequests: Array<{url, status, timestamp}>
  }
}
```

---

## Function Reference

### WebSocket Management

#### `connectWebSocket()`
**Purpose**: Establish and maintain WebSocket connection to server

**Flow**:
1. Check if connection already exists/connecting
2. Create new WebSocket to `ws://127.0.0.1:17892`
3. Set up event handlers (onopen, onmessage, onclose, onerror)
4. On open: send bridge_status, tabs info, active tab info, flush pending messages
5. On close: attempt reconnect after 1s delay

**Called by**: Startup, reconnection logic, extension installation
**Calls**: sendToServer(), sendTabsInfo(), sendActiveTabInfo(), flushPendingMessages(), sendHeartbeat()

---

#### `sendToServer(data)`
**Purpose**: Send JSON message to WebSocket server

**Parameters**:
- `data` (Object): Message payload

**Behavior**:
- If WebSocket open: send immediately
- If WebSocket not ready: queue in pendingMessages array

**Called by**: All functions sending data to server
**Calls**: ws.send()

---

#### `flushPendingMessages()`
**Purpose**: Send queued messages when WebSocket becomes ready

**Flow**:
1. Check if pendingMessages has items and WebSocket is open
2. Copy messages to temporary array and clear queue
3. Send each message via WebSocket
4. Log errors if sending fails

**Called by**: connectWebSocket() after connection established
**Calls**: ws.send()

---

### Keep-Alive System

#### `ensureKeepAlivePort()`
**Purpose**: Prevent Chrome from suspending the service worker

**Strategy**:
1. Check if keepAlivePorts already exist (return if yes)
2. Try creating offscreen document first
3. Fallback: inject keep-alive helper into accessible HTTP(S) tab
4. Helper creates persistent port connection named "ome_keep_alive"

**Called by**: Tab events, periodic check (30s interval), alarm handler
**Calls**: ensureOffscreenDocument(), chrome.scripting.executeScript()

---

#### `ensureOffscreenDocument()`
**Purpose**: Create offscreen document to host keep-alive port

**Flow**:
1. Check if chrome.offscreen API available
2. Check if document already exists
3. Create offscreen.html with TESTING reason
4. Used when no regular tabs available

**Called by**: ensureKeepAlivePort()
**Calls**: chrome.offscreen.createDocument()

---

#### `scheduleHeartbeatAlarm()`
**Purpose**: Create periodic alarm to wake service worker

**Parameters**: None
**Creates**: Alarm named "ome_ws_heartbeat" every 1 minute

**Called by**: Startup, installation
**Calls**: chrome.alarms.create()

---

#### `sendHeartbeat(reason)`
**Purpose**: Send ping to server to verify connection alive

**Parameters**:
- `reason` (string): Why heartbeat sent ("manual", "alarm", "onopen")

**Sends**:
```javascript
{
  type: "ping",
  source: "extension",
  reason: string,
  keepAlivePorts: number,
  timestamp: number
}
```

**Called by**: connectWebSocket(), alarm handler
**Calls**: sendToServer()

---

### Scan Orchestration

#### `requestScan(tabId, url, trigger)`
**Purpose**: Single entry point for ALL scan triggers - prevents duplicate scans

**Parameters**:
- `tabId` (number): Tab to scan
- `url` (string): Current URL
- `trigger` (string): Reason for scan ("url_change", "page_refresh", "tab_switch", "post_action", "post_scroll", "spa_navigation", etc.)

**Flow**:
1. Log scan request with counter and timestamp
2. Skip chrome:// URLs
3. Check if scan already in progress (dedupe)
4. Check if same URL already scanned (skip unless forced trigger)
5. Force scan triggers: post_action, significant_dom_change, page_refresh, tab_switch, post_scroll, inputPattern_submit
6. Mark scan in progress in tabState
7. Inject content script
8. Send 'start_scan' message to content script

**State Management**:
```javascript
tabState.set(tabId, {
  scanInProgress: true,
  lastUrl: url
});
```

**Called by**: Navigation listeners, tab activation, DOM command completion, action completion
**Calls**: chrome.scripting.executeScript(), chrome.tabs.sendMessage()

---

#### `handleScanComplete(message, sender)`
**Purpose**: Process scan completion from content script

**Flow**:
1. Get tabId from sender
2. Mark scan as complete in tabState (scanInProgress = false)
3. Forward intelligenceData to server via WebSocket

**Message sent to server**:
```javascript
{
  type: 'intelligence_update',
  data: message.intelligenceData
}
```

**Called by**: onMessage listener when type='scan_complete'
**Calls**: ws.send()

---

#### `triggerIntelligenceScan(tabId, url, reason)` **[DEPRECATED]**
**Purpose**: Legacy function - redirects to requestScan()

**Status**: Kept for compatibility, logs deprecation warning

---

### Tab Management

#### `sendTabsInfo(forceRefresh)`
**Purpose**: Query all tabs and send information to server

**Parameters**:
- `forceRefresh` (boolean): Whether to force refresh internal state

**Flow**:
1. Query all tabs via chrome.tabs.query({})
2. Map to tabsInfo array with id, url, title, active, status, pendingUrl
3. Update internal state via updateInternalTabState()
4. Send to server with type="tabs_info"
5. Also send active tab info

**Called by**: WebSocket connection, tab events, command handlers
**Calls**: chrome.tabs.query(), updateInternalTabState(), sendToServer(), sendActiveTabInfo()

---

#### `sendActiveTabInfo()`
**Purpose**: Send only the currently active tab information

**Flow**:
1. Query active tab in current window
2. Build activeTabInfo object
3. Send to server with type="active_tab_info"

**Message sent**:
```javascript
{
  type: "active_tab_info",
  activeTab: {
    id: number,
    url: string,
    title: string,
    status: string,
    pendingUrl: string
  },
  timestamp: number
}
```

**Called by**: WebSocket connection, tab activation, tab updates
**Calls**: chrome.tabs.query(), sendToServer()

---

#### `updateInternalTabState(tabsInfo, forceRefresh)`
**Purpose**: Maintain enhanced internal state for all tabs

**Parameters**:
- `tabsInfo` (Array): Array of tab information objects
- `forceRefresh` (boolean): Clear all state before updating

**Flow**:
1. If forceRefresh: clear internalTabState and tabCache
2. For each tab in tabsInfo:
   - Compare with old state
   - If changed (url, title, active, status):
     - Clear cache for tab
     - Update internal state with enhanced tracking
     - Initialize domChanges, siteConfig, networkActivity tracking
   - If new active tab: ensure content script fresh
   - If URL changed: force content script refresh
3. Remove tabs that no longer exist

**Called by**: sendTabsInfo()
**Calls**: clearTabCache(), ensureContentScriptFresh()

---

#### `clearTabCache(tabId)`
**Purpose**: Clear cached data for specific tab

**Parameters**:
- `tabId` (number): Tab to clear

**Flow**:
1. Remove entry from tabCache Map
2. Mark tab as needsFreshScan in internalTabState
3. Set lastCacheClear timestamp

**Called by**: Navigation, tab updates, force refresh
**Calls**: None

---

#### `ensureContentScriptFresh(tabId)`
**Purpose**: Force re-injection of content script into tab

**Parameters**:
- `tabId` (number): Tab to refresh

**Flow**:
1. Check if actionInProgress (skip if true to prevent interrupting actions)
2. Inject content.js via chrome.scripting.executeScript
3. Proactively send site config after injection (calls proactivelySendSiteConfig - **NOTE: Function called but not defined**)
4. Mark tab as having fresh content script
5. On error: mark refresh as failed

**Called by**: Tab state updates, active tab changes, DOM command handlers
**Calls**: chrome.scripting.executeScript(), proactivelySendSiteConfig() (undefined)

---

#### `findActiveTab()`
**Purpose**: Find currently active accessible tab with multiple fallback strategies

**Returns**: Promise<Tab> or null

**Strategy**:
1. **Strategy 1**: Find active tab in current window
   - Filter for accessible (not chrome://)
   - Get refreshed tab info
2. **Strategy 2**: Find active tab in last focused window
3. **Strategy 3**: Find any accessible tab in current window
   - Sort by priority (non-chrome first, then active)
4. **Last Resort**: Find any visible accessible tab across all windows
   - Sort by active status

**Called by**: All command handlers needing active tab
**Calls**: chrome.tabs.query(), chrome.tabs.get(), chrome.windows.getCurrent(), isTabAccessible()

---

#### `isTabAccessible(tab)`
**Purpose**: Check if tab is accessible for content scripts

**Parameters**:
- `tab` (Object): Tab object

**Returns**: boolean

**Checks**:
- Has URL
- Not chrome://, chrome-extension://, about:, edge://, moz-extension://
- Not about:blank

**Called by**: findActiveTab()

---

### Message Handling

#### `handleServerMessage(messageData)`
**Purpose**: Central dispatcher for WebSocket messages from server

**Parameters**:
- `messageData` (string): Raw JSON message

**Routes to**:
- `site_configs_update` → Store configs, forward to content scripts
- `execute_llm_action` → handleExecuteLLMAction()
- `execute_capability` → handleExecuteCapability()
- `server_ping` → Send pong response
- Command messages (command + id) → Route by command type:
  - `navigate` → handleNavigateCommand()
  - `switchTab` → handleSwitchTabCommand()
  - `openTab` → handleOpenTabCommand()
  - `closeTab` → handleCloseTabCommand()
  - `updateTabUrl` → handleUpdateTabUrlCommand()
  - DOM commands → handleDOMCommand()

**Called by**: WebSocket onmessage event
**Calls**: Various command handlers

---

#### Chrome Runtime Message Listener
**Handles internal messages from**:
- Popup
- Content scripts
- Other extension components

**Message types**:
- `setWsUrl` → handleSetWsUrl()
- `forceRefresh` → handleForceRefresh()
- `clearAllCache` → handleClearAllCache()
- `getStatus` → handleGetStatus()
- `dom_changed` → handleDOMChanged()
- `network_activity` → handleNetworkActivity()
- `intelligence_update` → handleIntelligenceUpdate()
- `scan_complete` → handleScanComplete()
- `request_scan` → requestScan()
- `get_site_config_for_domain` → handleGetSiteConfigForDomain()
- `execute_llm_action` → handleExecuteLLMAction()
- `ping` → Simple pong response
- `force_content_script_reinjection` → handleForceContentScriptReinjection()
- `force_extension_reload` → handleForceExtensionReload()
- `immediate_scan_results` → handleImmediateScanResults()

**Returns**: true (keeps async channel open)

---

### Command Handlers - Navigation

#### `handleNavigateCommand(message)`
**Purpose**: Navigate active tab to new URL

**Message format**:
```javascript
{
  id: string,
  command: "navigate",
  params: { url: string }
}
```

**Flow**:
1. Extract URL from params
2. Find active tab
3. Update tab URL via chrome.tabs.update
4. Clear tab cache
5. Send success response

**Called by**: handleServerMessage()
**Calls**: findActiveTab(), chrome.tabs.update(), clearTabCache(), sendSuccessResponse()

---

### Command Handlers - Tab Control

#### `handleSwitchTabCommand(message)`
**Purpose**: Switch to specific tab by ID

**Message format**:
```javascript
{
  id: string,
  command: "switchTab",
  params: { tabId: number }
}
```

**Flow**:
1. Extract tabId from params
2. Activate tab via chrome.tabs.update with active: true
3. Get tab info
4. Send active tab and all tabs info to server
5. Send success response

**Called by**: handleServerMessage(), handleTabCapability()
**Calls**: chrome.tabs.update(), chrome.tabs.get(), sendActiveTabInfo(), sendTabsInfo(), sendSuccessResponse()

---

#### `handleOpenTabCommand(message)`
**Purpose**: Open new browser tab

**Message format**:
```javascript
{
  id: string,
  command: "openTab",
  params: { url?: string }
}
```

**Flow**:
1. Extract URL from params (optional)
2. Create new tab via chrome.tabs.create
3. Send updated tabs info to server
4. Send success response with new tab info

**Called by**: handleServerMessage(), handleTabCapability()
**Calls**: chrome.tabs.create(), sendActiveTabInfo(), sendTabsInfo(), sendSuccessResponse()

---

#### `handleCloseTabCommand(message)`
**Purpose**: Close specific tab

**Message format**:
```javascript
{
  id: string,
  command: "closeTab",
  params: { tabId: number }
}
```

**Flow**:
1. Extract tabId from params
2. Close tab via chrome.tabs.remove
3. Send updated tabs info
4. Send success response

**Called by**: handleServerMessage(), handleTabCapability()
**Calls**: chrome.tabs.remove(), sendActiveTabInfo(), sendTabsInfo(), sendSuccessResponse()

---

#### `handleUpdateTabUrlCommand(message)`
**Purpose**: Navigate specific tab to new URL

**Message format**:
```javascript
{
  id: string,
  command: "updateTabUrl",
  params: { tabId: number, url: string }
}
```

**Flow**:
1. Extract tabId and url from params
2. Update tab URL via chrome.tabs.update
3. Clear cache for tab
4. Get updated tab info
5. Send tabs info to server
6. Send success response

**Called by**: handleServerMessage(), handleTabCapability()
**Calls**: chrome.tabs.update(), clearTabCache(), chrome.tabs.get(), sendActiveTabInfo(), sendTabsInfo(), sendSuccessResponse()

---

### Command Handlers - DOM Operations

#### `handleDOMCommand(message)`
**Purpose**: Forward DOM commands to content script in active tab

**Handles commands**:
- waitFor, getText, click, getPageMarkdown, extractPageText
- scroll (page-by-page viewport scrolling)
- getCurrentTabInfo, getNavigationContext, searchActions
- discoverLoginControls, generateSiteMap, scanAndRegisterElements
- navigateBack, navigateForward, jumpToHistoryEntry
- getHistoryState, searchHistory, clearHistory

**Flow**:
1. Find active tab
2. Check if content script needs refresh (needsFreshScan or !contentScriptFresh)
3. Inject content script if needed
4. Proactively send site config after injection (calls undefined function)
5. Wait 100ms for content script initialization
6. Send message to content script via chrome.tabs.sendMessage
7. Handle response:
   - If error: send error response
   - If success: send success response
   - If scroll command: trigger post_scroll scan after 300ms

**Called by**: handleServerMessage()
**Calls**: findActiveTab(), ensureContentScriptFresh(), chrome.scripting.executeScript(), proactivelySendSiteConfig() (undefined), chrome.tabs.sendMessage(), sendSuccessResponse(), sendErrorResponse(), requestScan()

---

### Command Handlers - Internal Extension

#### `handleSetWsUrl(message, sendResponse)`
**Purpose**: Update WebSocket URL from popup

**Flow**:
1. Store new URL in chrome.storage.local
2. Close existing WebSocket
3. Reconnect with new URL after 100ms

**Called by**: onMessage listener
**Calls**: chrome.storage.local.set(), connectWebSocket()

---

#### `handleForceRefresh(message, sendResponse)`
**Purpose**: Force refresh all tabs and content scripts

**Flow**:
1. Force refresh all internal state via sendTabsInfo(true)
2. Query all tabs
3. For each tab: call ensureContentScriptFresh()
4. Count successful refreshes
5. Send response with count

**Called by**: onMessage listener (popup request)
**Calls**: sendTabsInfo(), chrome.tabs.query(), ensureContentScriptFresh()

---

#### `handleClearAllCache(message, sendResponse)`
**Purpose**: Clear all cached data for all tabs

**Flow**:
1. Iterate tabCache entries, call clearTabCache() for each
2. Mark all tabs in internalTabState as needsFreshScan
3. Send response with count

**Called by**: onMessage listener (popup request)
**Calls**: clearTabCache()

---

#### `handleGetStatus(message, sendResponse)`
**Purpose**: Return current extension status metrics

**Returns**:
```javascript
{
  ok: true,
  result: {
    isConnected: boolean,
    totalTabs: number,
    tabsWithFreshScripts: number,
    tabsNeedingFreshScan: number,
    tabsWithCacheIssues: number,
    tabsWithDOMChanges: number,
    totalDOMChanges: number,
    recentDOMChanges: number,
    tabsWithSiteConfigs: number,
    totalSiteConfigs: number,
    lastActiveTabId: number,
    websocketState: string,
    timestamp: number
  }
}
```

**Called by**: onMessage listener (popup request)

---

#### `handleDOMChanged(message, sendResponse)`
**Purpose**: Process DOM change notifications from content scripts

**Message data**:
```javascript
{
  changeNumber: number,
  types: string[],
  totalMutations: number,
  url: string,
  timestamp: number,
  isSignificant: boolean
}
```

**Flow**:
1. Find tab by URL in internalTabState
2. Update domChanges tracking:
   - totalChanges
   - lastChangeTime
   - lastMutationCount
   - changeTypes Set
3. Mark tab as needsFreshScan
4. If totalMutations > 10: send dom_content_changed to server
5. **Note**: Full rescans on DOM mutations are DISABLED - content.js handles incrementally

**Called by**: onMessage listener from content script
**Calls**: sendToServer()

---

#### `handleNetworkActivity(message, sender)`
**Purpose**: Track network requests from content scripts

**Message data**:
```javascript
{
  eventType: string,
  url: string,
  status: number,
  inflightRequests: number,
  timestamp: number
}
```

**Flow**:
1. Get tabId from sender
2. Update networkActivity in internalTabState:
   - inflightRequests count
   - lastActivity timestamp
   - recentRequests array (last 10)
3. Send network_activity message to server
4. If network idle (inflightRequests === 0) and not actionInProgress:
   - Schedule rescan after 1s delay (**DISABLED** to prevent action ID invalidation)

**Called by**: onMessage listener from content script
**Calls**: sendToServer(), triggerIntelligenceScan() (disabled)

---

#### `handleIntelligenceUpdate(message, sender, sendResponse)`
**Purpose**: Forward intelligence updates from content script to server

**Validation**:
1. Check message has data
2. Check sender has tab context
3. Verify update is from currently active tab (skip if inactive)

**Flow**:
1. Extract intelligenceData from message
2. Attach tab metadata (tabId, tabUrl, tabTitle)
3. Validate actionableElements exists and is array
4. Send to server via WebSocket with type="intelligence_update"

**Message to server**:
```javascript
{
  type: "intelligence_update",
  tabId: number,
  tabUrl: string,
  tabTitle: string,
  data: {
    pageState: object,
    actionableElements: array,
    recentInsights: array,
    totalEvents: number,
    actionMapping: object,
    timestamp: number,
    tabId: number,
    tabUrl: string,
    tabTitle: string
  }
}
```

**Called by**: onMessage listener from content script
**Calls**: findActiveTab(), ws.send()

---

### Action Execution

#### `handleExecuteLLMAction(message, sendResponse)`
**Purpose**: Execute LLM-generated actions on page elements

**Message data**:
```javascript
{
  type: "execute_llm_action",
  data: {
    actionId: string,
    actionType: string,
    params: object
  }
}
```

**Flow**:
1. Set actionInProgress = true (prevents content script refresh during action)
2. Find active tab
3. Send execute_action message to content script
4. If successful:
   - Send success response
   - Wait 1s, then trigger post_action scan via requestScan()
5. If failed:
   - Clear actionInProgress flag
   - Send error response
6. On exception:
   - Clear actionInProgress flag
   - Send error response

**State coordination**:
- Sets actionInProgress = true at start
- Clears on error/exception
- Note: **actionInProgress variable is used but not declared** (potential bug)

**Called by**: handleServerMessage(), onMessage listener
**Calls**: findActiveTab(), chrome.tabs.sendMessage(), requestScan()

---

### Capability Execution

#### `handleExecuteCapability(message)`
**Purpose**: Smart dispatcher for capability commands

**Message format**:
```javascript
{
  id: string,
  type: "execute_capability",
  action: string,
  params: object
}
```

**Routing logic**:
- **Tab capabilities** (browser-level) → handleTabCapability():
  - SwitchTab, OpenTab, CloseTab, UpdateTabURL
- **DOM capabilities** (page-level) → handleDOMCapability():
  - RetrieveTranscript, TogglePlayPause, etc.

**Called by**: handleServerMessage()
**Calls**: handleTabCapability() or handleDOMCapability()

---

#### `handleTabCapability(action, params, requestId)`
**Purpose**: Execute tab manipulation capabilities via Chrome APIs

**Adapts capability format to existing handlers**:
```javascript
const adaptedMessage = {
  id: requestId,
  params: params
};
```

**Routes to**:
- SwitchTab → handleSwitchTabCommand()
- OpenTab → handleOpenTabCommand()
- CloseTab → handleCloseTabCommand()
- UpdateTabURL → handleUpdateTabUrlCommand()

**Called by**: handleExecuteCapability()
**Calls**: Tab command handlers, sendErrorResponse()

---

#### `handleDOMCapability(action, params, requestId)`
**Purpose**: Forward DOM capabilities to content script

**Message to content script**:
```javascript
{
  type: "execute_capability",
  action: string,
  params: object
}
```

**Flow**:
1. Find active tab
2. Send capability message to content script
3. Handle response:
   - Success: send success response with result
   - Failure: send error response

**Called by**: handleExecuteCapability()
**Calls**: findActiveTab(), chrome.tabs.sendMessage(), sendSuccessResponse(), sendErrorResponse()

---

### Response Helpers

#### `sendSuccessResponse(id, result)`
**Purpose**: Send success response to server

**Message format**:
```javascript
{
  id: string,
  ok: true,
  result: object,
  error: null
}
```

**Called by**: All command handlers on success
**Calls**: sendToServer()

---

#### `sendErrorResponse(id, code, msg)`
**Purpose**: Send error response to server

**Message format**:
```javascript
{
  id: string,
  ok: false,
  result: null,
  error: {
    code: string,
    msg: string
  }
}
```

**Called by**: All command handlers on error
**Calls**: sendToServer()

---

### Site Configuration

#### `loadSiteConfigsFromStorage()`
**Purpose**: Load site configs on service worker startup

**Flow**:
1. Get siteConfigs from chrome.storage.local
2. Store in global siteConfigs object
3. Log available domains

**Called by**: Startup initialization (line 2712 shows function is defined but call site not shown in read portion)
**Calls**: chrome.storage.local.get()

---

#### `handleGetSiteConfigForDomain(message, sendResponse)`
**Purpose**: Lookup site config for specific domain

**Matching logic**:
1. Check exact domain match
2. Check wildcard patterns (*.example.com)
3. Check subdomain matches (ends with .configDomain)
4. Fallback to 'default' config

**Sorting**: Domains sorted by length (longer = more specific = higher priority)

**Called by**: onMessage listener from content script
**Calls**: chrome.storage.local.get()

---

### Orb & HUD System

#### `handleToggleHUD(message)`
**Purpose**: Toggle the HUD (heads-up display) overlay interface on the active tab

**Parameters**:
- `message` (Object): Message with optional `id` property

**Flow**:
1. Generate requestId from message.id or create with timestamp
2. Find active tab via findActiveTab()
3. Forward toggle_hud message to content script
4. Content script handles orb and HUD visibility
5. Send hud_response back to server with result

**Message to server**:
```javascript
{
  type: "hud_response",
  id: requestId,
  ok: boolean,
  result: object | null,
  error: string | null
}
```

**Called by**: handleServerMessage() when type="toggle_hud"
**Calls**: findActiveTab(), chrome.tabs.sendMessage(), sendToServer()

---

#### `handleSetOrbTheme(message)`
**Purpose**: Change the orb theme on the active tab

**Parameters**:
- `message` (Object): Message with `id` and `theme` properties

**Flow**:
1. Generate requestId from message.id or create with timestamp
2. Find active tab
3. Forward set_orb_theme message to content script with theme name
4. Send orb_theme_response back to server

**Message to server**:
```javascript
{
  type: "orb_theme_response",
  id: requestId,
  ok: boolean,
  result: object | null,
  error: string | null
}
```

**Called by**: handleServerMessage() when type="set_orb_theme"
**Calls**: findActiveTab(), chrome.tabs.sendMessage(), sendToServer()

---

#### `handleGetOrbThemes(message)`
**Purpose**: Get list of available orb themes from content script

**Parameters**:
- `message` (Object): Message with optional `id` property

**Flow**:
1. Generate requestId from message.id or create with timestamp
2. Find active tab
3. Forward get_orb_themes message to content script
4. Content script returns list of available themes
5. Send orb_themes_response back to server

**Message to server**:
```javascript
{
  type: "orb_themes_response",
  id: requestId,
  ok: boolean,
  result: { themes: string[] } | null,
  error: string | null
}
```

**Called by**: handleServerMessage() when type="get_orb_themes"
**Calls**: findActiveTab(), chrome.tabs.sendMessage(), sendToServer()

---

### Chat System

The service worker acts as a bidirectional bridge for the chat system, routing messages between the WebSocket server and content script's HUD chat interface.

#### Chat Message Flow: User → Server

**Internal message handler**: `ui_chat_user_message`

**Purpose**: Forward user chat messages from HUD to WebSocket server

**Flow**:
1. Content script sends ui_chat_user_message to service worker
2. Extract chat_id, prompt, page_url, page_title, front_end_context
3. Build WebSocket payload with type="chat_user_message"
4. Send to server via sendToServer()
5. Return success response to content script

**Message to server**:
```javascript
{
  type: "chat_user_message",
  chat_id: string | null,
  data: {
    prompt: string,
    page_url: string,
    page_title: string,
    front_end_context: object
  }
}
```

**Called by**: onMessage listener from content script
**Calls**: sendToServer()

---

#### Chat History Request: UI → Server

**Internal message handler**: `ui_get_chat_history`

**Purpose**: Request chat history for a specific chat_id from server

**Flow**:
1. Content script sends ui_get_chat_history with chat_id
2. Build WebSocket payload with type="get_chat_history"
3. Send to server via sendToServer()
4. Server will respond asynchronously with chat_history message

**Message to server**:
```javascript
{
  type: "get_chat_history",
  chat_id: string | null
}
```

**Called by**: onMessage listener from content script
**Calls**: sendToServer()

---

#### Chat Response: Server → UI

**WebSocket message handler**: `chat_append_ack`

**Purpose**: Forward server acknowledgment of appended chat message to HUD

**Flow**:
1. Server sends chat_append_ack via WebSocket
2. Service worker receives and logs
3. Query active tab
4. Forward to content script as ui_chat_append_ack
5. Content script updates HUD with acknowledgment

**Message to content script**:
```javascript
{
  type: "ui_chat_append_ack",
  data: {
    // Server acknowledgment data
  }
}
```

**Called by**: handleServerMessage() when type="chat_append_ack"
**Calls**: chrome.tabs.query(), chrome.tabs.sendMessage()

---

#### Chat Error: Server → UI

**WebSocket message handler**: `chat_error`

**Purpose**: Forward chat errors from server to HUD

**Flow**:
1. Server sends chat_error via WebSocket
2. Service worker receives and logs
3. Query active tab
4. Forward to content script as ui_chat_error
5. Content script displays error in HUD

**Message to content script**:
```javascript
{
  type: "ui_chat_error",
  data: {
    // Server error data
  }
}
```

**Called by**: handleServerMessage() when type="chat_error"
**Calls**: chrome.tabs.query(), chrome.tabs.sendMessage()

---

#### Chat History Response: Server → UI

**WebSocket message handler**: `chat_history`

**Purpose**: Forward chat history from server to HUD

**Flow**:
1. Server sends chat_history via WebSocket (in response to get_chat_history)
2. Service worker receives and logs chat_id and message count
3. Query active tab
4. Forward to content script as ui_chat_history
5. Content script populates HUD chat with messages

**Message to content script**:
```javascript
{
  type: "ui_chat_history",
  data: {
    chat_id: string,
    messages: array
  }
}
```

**Called by**: handleServerMessage() when type="chat_history"
**Calls**: chrome.tabs.query(), chrome.tabs.sendMessage()

---

### Orb State Management

#### Internal Message: `get_orb_state`

**Purpose**: Return current orb state to content script (includes zoom level)

**Flow**:
1. Content script requests orb state
2. Find active tab
3. Get zoom level for tab via chrome.tabs.getZoom()
4. Return orbState object + zoom
5. Fallback to zoom=1.0 if error

**Response**:
```javascript
{
  ok: true,
  theme: string,
  position: { left: number, top: number } | null,
  chatVisible: boolean,
  chatInput: string,
  chatPanelSize: { width: number, height: number } | null,
  zoom: number
}
```

**Called by**: onMessage listener from content script
**Calls**: findActiveTab(), chrome.tabs.getZoom()

---

#### Internal Message: `set_orb_state`

**Purpose**: Update orb state from content script (persists state across navigations)

**Flow**:
1. Content script sends updated orb state
2. Update orbState object properties:
   - theme (if provided)
   - position (if provided)
   - chatVisible (if provided)
   - chatInput (if provided)
   - chatPanelSize (if provided)
3. If chatPanelSize updated: persist to chrome.storage.local
4. Log updated state
5. Return success response

**State persistence**: chatPanelSize is saved to chrome.storage.local to survive browser restarts

**Called by**: onMessage listener from content script
**Calls**: chrome.storage.local.set()

---

#### Internal Message: `set_orb_theme` (from popup)

**Purpose**: Set orb theme from popup UI and synchronize with content script

**Flow**:
1. Popup sends set_orb_theme with theme name
2. Validate theme name exists
3. Update orbState.theme
4. Update extension toolbar icon based on theme:
   - Map theme to icon prefix (robot → ome, others → theme name)
   - Update chrome.action.setIcon() with 16/48/128 variants
5. Forward to active tab's content script as apply_orb_theme
6. Persist theme to chrome.storage.local as orbTheme
7. Return success response

**Message to content script**:
```javascript
{
  type: "apply_orb_theme",
  theme: string
}
```

**Called by**: onMessage listener from popup
**Calls**: chrome.action.setIcon(), chrome.tabs.query(), chrome.tabs.sendMessage(), chrome.storage.local.set()

---

### Tear Away System

#### `handleForceContentScriptReinjection(message, sendResponse)`
**Purpose**: Force re-inject content script (for CSP bypass scenarios)

**Message data**:
```javascript
{
  data: {
    tabId?: number,
    reason: string,
    timestamp: number
  }
}
```

**Flow**:
1. Get tabId or find current active tab (calls getCurrentActiveTabId - **NOTE: Function not defined**)
2. Force content script refresh via ensureContentScriptFresh()
3. Clear tab cache
4. Mark tab as needsFreshScan
5. Send success response

**Called by**: onMessage listener
**Calls**: getCurrentActiveTabId() (undefined), ensureContentScriptFresh(), clearTabCache()

---

#### `handleForceExtensionReload(message, sendResponse)`
**Purpose**: Force extension reload (requires manual user action)

**Message data**:
```javascript
{
  data: {
    reason: string,
    timestamp: number,
    forceReload?: boolean
  }
}
```

**Flow**:
1. Clear all internal state
2. Close WebSocket
3. If forceReload:
   - Query all active tabs
   - Hard refresh each (bypassCache: true)
   - Schedule content script re-injection after 2s
4. Send response with instructions

**Note**: Actual extension reload requires manual action at chrome://extensions/

**Called by**: onMessage listener
**Calls**: chrome.tabs.query(), chrome.tabs.reload(), chrome.scripting.executeScript()

---

#### `handleImmediateScanResults(message, sendResponse)`
**Purpose**: Process and forward immediate scan results after tear away

**Message data**:
```javascript
{
  data: {
    scanType: string,
    elementCounts: object,
    frameContext: object,
    scanDuration: number,
    tearAwaySuccess: boolean
  }
}
```

**Flow**:
1. Log scan result summary
2. Send to server via WebSocket with type="immediate_scan_results_after_tear_away"
3. If WebSocket not available: queue in pendingMessages

**Called by**: onMessage listener from content script
**Calls**: ws.send() or queues in pendingMessages

---

## Event Listeners

### Chrome Tab Events

#### `chrome.tabs.onActivated`
**Purpose**: Handle tab activation (user switches tabs)

**Flow**:
1. Clear cache for previously active tab
2. Update lastActiveTabId
3. Force inject content script into newly active tab
4. Mark as having fresh content script
5. Send active tab info and tabs info to server
6. Trigger scan with trigger='tab_switch'

**Calls**: clearTabCache(), chrome.scripting.executeScript(), sendActiveTabInfo(), sendTabsInfo(), requestScan()

---

#### `chrome.tabs.onUpdated`
**Purpose**: Handle tab updates (URL or title changes)

**Flow**:
1. If URL or title changed:
   - Send active tab info if tab is active
   - Send all tabs info
2. Ensure keep-alive port exists

**Calls**: sendActiveTabInfo(), sendTabsInfo(), ensureKeepAlivePort()

---

#### `chrome.tabs.onCreated`
**Purpose**: Handle new tab creation

**Flow**:
1. Send active tab info
2. Send all tabs info
3. Ensure keep-alive port

**Calls**: sendActiveTabInfo(), sendTabsInfo(), ensureKeepAlivePort()

---

#### `chrome.tabs.onRemoved`
**Purpose**: Handle tab closure

**Flow**:
1. Delete from tabState Map
2. Delete from tabScanState Map (legacy)
3. Send updated tab info
4. Ensure keep-alive port if none exist

**Calls**: sendActiveTabInfo(), sendTabsInfo(), ensureKeepAlivePort()

---

### Chrome Navigation Events

#### `chrome.webNavigation.onBeforeNavigate`
**Purpose**: Clear scan state before navigation

**Filter**: Main frame only (frameId === 0), http/https schemes

**Flow**: Delete tabId from tabScanState Map

---

#### `chrome.webNavigation.onCommitted`
**Purpose**: Detect page refresh (F5)

**Filter**: Main frame only

**Flow**:
1. Check if transitionType is 'reload' or has 'forward_back' qualifier
2. If refresh: mark isRefresh=true in tabState for this tab

**Used by**: onCompleted to determine trigger type

---

#### `chrome.webNavigation.onCompleted`
**Purpose**: Trigger scan after page load

**Filter**: Main frame only, http/https schemes

**Flow**:
1. Check if isRefresh flag set
2. Determine trigger: "page_refresh" or "url_change"
3. Clear isRefresh flag
4. Call requestScan()

**Calls**: requestScan()

---

#### `chrome.webNavigation.onHistoryStateUpdated`
**Purpose**: Handle SPA navigation (history API changes)

**Filter**: Main frame only, http/https schemes

**Flow**: Call requestScan() with trigger="spa_navigation"

**Calls**: requestScan()

---

### Chrome Runtime Events

#### `chrome.runtime.onStartup`
**Purpose**: Handle browser startup

**Flow**:
1. Connect WebSocket
2. Ensure keep-alive port
3. Schedule heartbeat alarm

**Calls**: connectWebSocket(), ensureKeepAlivePort(), scheduleHeartbeatAlarm()

---

#### `chrome.runtime.onInstalled`
**Purpose**: Handle extension installation or update

**Flow**:
1. Connect WebSocket
2. Ensure keep-alive port
3. Schedule heartbeat alarm

**Calls**: connectWebSocket(), ensureKeepAlivePort(), scheduleHeartbeatAlarm()

---

#### `chrome.runtime.onConnect`
**Purpose**: Track keep-alive port connections

**Filter**: Only handles ports named "ome_keep_alive"

**Flow**:
1. Add port to keepAlivePorts Set
2. On disconnect:
   - Remove from Set
   - Call ensureKeepAlivePort() to recreate

**Calls**: ensureKeepAlivePort()

---

#### `chrome.alarms.onAlarm`
**Purpose**: Handle periodic heartbeat alarm

**Filter**: Only handles "ome_ws_heartbeat" alarm

**Flow**:
1. Send heartbeat with reason="alarm"
2. Ensure keep-alive port exists

**Calls**: sendHeartbeat(), ensureKeepAlivePort()

---

## Workflows and Pipelines

### 1. Standard Action Execution Pipeline

```
┌────────────────────────────────────────────────────────────────┐
│ 1. Test Client sends action request to WebSocket Server       │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 2. Server forwards to Service Worker via WebSocket            │
│    Message: { type: "execute_llm_action", data: {...} }       │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 3. handleExecuteLLMAction() processes:                         │
│    - Set actionInProgress = true                               │
│    - Find active tab                                           │
│    - Forward to content script                                 │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 4. Content script executes action on DOM                       │
│    - Returns { ok: true, result: {...} }                       │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 5. Service Worker receives response:                           │
│    - Send success response to server                           │
│    - Wait 1s, trigger post_action scan                         │
│    - Clear actionInProgress flag (via scan completion)         │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 6. Server routes response back to Test Client                  │
└────────────────────────────────────────────────────────────────┘
```

---

### 2. Capability Execution Pipeline

```
┌────────────────────────────────────────────────────────────────┐
│ 1. Server sends capability command                             │
│    Message: { type: "execute_capability", action: "...", ...} │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 2. handleExecuteCapability() - Smart Dispatcher                │
│    Checks action type:                                         │
│    - Tab capability? (SwitchTab, OpenTab, etc.)                │
│    - DOM capability? (RetrieveTranscript, etc.)                │
└────────┬───────────────────────────────┬──────────────────────┘
         │                               │
         ▼                               ▼
┌──────────────────────┐      ┌─────────────────────────────────┐
│ handleTabCapability  │      │ handleDOMCapability             │
│ (Chrome APIs)        │      │ (Content Script)                │
│                      │      │                                 │
│ - switchTab()        │      │ Forward to content script:      │
│ - openTab()          │      │ - RetrieveTranscript            │
│ - closeTab()         │      │ - TogglePlayPause               │
│ - updateTabUrl()     │      │ - etc.                          │
└──────────┬───────────┘      └─────────────┬───────────────────┘
           │                                 │
           └─────────────┬───────────────────┘
                         ▼
┌────────────────────────────────────────────────────────────────┐
│ 3. Send response back to server with requestId                 │
└────────────────────────────────────────────────────────────────┘
```

---

### 3. Scan Orchestration Flow

```
┌────────────────────────────────────────────────────────────────┐
│ Trigger Source (one of):                                       │
│ - URL change (webNavigation.onCompleted)                       │
│ - Page refresh (F5)                                            │
│ - Tab switch (tabs.onActivated)                                │
│ - SPA navigation (webNavigation.onHistoryStateUpdated)         │
│ - Post-action (after LLM action completes)                     │
│ - Post-scroll (after scroll command)                           │
│ - Content script request (request_scan message)                │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ requestScan(tabId, url, trigger)                               │
│                                                                 │
│ Deduplication checks:                                          │
│ 1. Skip chrome:// URLs                                         │
│ 2. If scan in progress → skip                                  │
│ 3. If same URL already scanned → skip                          │
│    UNLESS: forced trigger (post_action, page_refresh, etc.)    │
│                                                                 │
│ Actions:                                                        │
│ 1. Mark scanInProgress = true                                  │
│ 2. Inject content script                                       │
│ 3. Send 'start_scan' message                                   │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ Content Script performs scan                                   │
│ - Walks DOM tree                                               │
│ - Registers actionable elements                                │
│ - Generates artifacts                                          │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ Content Script sends 'scan_complete' message                   │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ handleScanComplete(message, sender)                            │
│ 1. Mark scanInProgress = false                                 │
│ 2. Forward intelligenceData to server                          │
└────────────────────────────────────────────────────────────────┘
```

---

### 4. Tab Lifecycle Management

```
┌─────────────────────────────────────────────────────────────┐
│ Tab Created (tabs.onCreated)                                │
│ → Send tabs info to server                                  │
│ → Ensure keep-alive port                                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Tab Activated (tabs.onActivated)                            │
│ 1. Clear cache for previous tab                             │
│ 2. Inject content script into new active tab                │
│ 3. Send active tab info                                     │
│ 4. Trigger scan with trigger='tab_switch'                   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Tab Updated (tabs.onUpdated)                                │
│ → If URL/title changed: send tabs info                      │
│ → Ensure keep-alive port                                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Tab Removed (tabs.onRemoved)                                │
│ 1. Delete from tabState Map                                 │
│ 2. Delete from tabScanState Map                             │
│ 3. Send updated tabs info                                   │
│ 4. Ensure keep-alive port if needed                         │
└─────────────────────────────────────────────────────────────┘
```

---

### 5. Site Config Distribution

```
┌────────────────────────────────────────────────────────────────┐
│ 1. Server sends site_configs_update via WebSocket             │
│    Message: { type: "site_configs_update", data: {...} }      │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 2. handleServerMessage() processes:                            │
│    - Store in global siteConfigs object                        │
│    - Store in chrome.storage.local for persistence            │
│    - Forward to all content scripts                            │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 3. Query all tabs, send message to each:                       │
│    chrome.tabs.sendMessage(tabId, {                            │
│      type: "site_configs_update",                              │
│      data: siteConfigs                                         │
│    })                                                           │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│ Lookup Flow (content script requests config):                  │
│ 1. Content sends 'get_site_config_for_domain'                 │
│ 2. handleGetSiteConfigForDomain() looks up:                    │
│    - Exact match                                               │
│    - Wildcard match (*.example.com)                            │
│    - Subdomain match                                           │
│    - Fallback to 'default'                                     │
│ 3. Return config or null                                       │
└────────────────────────────────────────────────────────────────┘
```

---

### 6. Keep-Alive Mechanism

```
┌────────────────────────────────────────────────────────────────┐
│ Purpose: Prevent Chrome from suspending service worker         │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ Strategy 1: Persistent Port Connection                         │
│ - Content scripts connect port named "ome_keep_alive"          │
│ - Service worker tracks in keepAlivePorts Set                  │
│ - As long as one port exists, worker stays alive               │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ Strategy 2: Offscreen Document                                 │
│ - Create offscreen.html when no tabs available                 │
│ - Hosts keep-alive port connection                             │
│ - Used when only chrome:// tabs open                           │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ Strategy 3: Periodic Heartbeat Alarm                           │
│ - Alarm every 1 minute named "ome_ws_heartbeat"                │
│ - Wakes service worker if suspended                            │
│ - Sends ping to server                                         │
│ - Checks keep-alive port exists                                │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ Strategy 4: Periodic Check                                     │
│ - setInterval every 30 seconds                                 │
│ - If keepAlivePorts.size === 0:                                │
│   - Call ensureKeepAlivePort()                                 │
│   - Attempts to inject into accessible tab                     │
└────────────────────────────────────────────────────────────────┘
```

---

### 7. Chat Pipeline (Bidirectional)

The service worker bridges the chat system between the HUD UI (content script) and the WebSocket server.

```
┌────────────────────────────────────────────────────────────────┐
│ USER → SERVER (Chat message flow)                              │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 1. User types in HUD chat input, submits                       │
│    Content script collects:                                    │
│    - prompt (user text)                                        │
│    - page_url, page_title                                      │
│    - front_end_context (additional metadata)                   │
│    - chat_id (conversation ID, null for new chat)              │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 2. Content script → Service Worker                             │
│    chrome.runtime.sendMessage({                                │
│      type: 'ui_chat_user_message',                             │
│      chat_id: string | null,                                   │
│      data: { prompt, page_url, page_title, front_end_context } │
│    })                                                           │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 3. Service Worker → WebSocket Server                           │
│    sendToServer({                                              │
│      type: 'chat_user_message',                                │
│      chat_id: string | null,                                   │
│      data: { prompt, page_url, page_title, front_end_context } │
│    })                                                           │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 4. Server processes message, appends to chat history           │
│    Invokes LLM for response generation                         │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│ SERVER → USER (Chat response flow)                             │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 5. WebSocket Server → Service Worker                           │
│    ws.send({                                                    │
│      type: 'chat_append_ack',                                  │
│      data: { ... acknowledgment ... }                          │
│    })                                                           │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 6. Service Worker → Content Script                             │
│    chrome.tabs.sendMessage(activeTab.id, {                     │
│      type: 'ui_chat_append_ack',                               │
│      data: message                                             │
│    })                                                           │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 7. Content script updates HUD with acknowledgment              │
│    Shows user message in chat UI                               │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│ CHAT HISTORY REQUEST (on HUD open)                             │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 1. Content script → Service Worker                             │
│    chrome.runtime.sendMessage({                                │
│      type: 'ui_get_chat_history',                              │
│      chat_id: string | null                                    │
│    })                                                           │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 2. Service Worker → Server                                     │
│    sendToServer({ type: 'get_chat_history', chat_id })         │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 3. Server → Service Worker                                     │
│    ws.send({                                                    │
│      type: 'chat_history',                                     │
│      chat_id: string,                                          │
│      messages: [ ... array of messages ... ]                   │
│    })                                                           │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 4. Service Worker → Content Script                             │
│    chrome.tabs.sendMessage(activeTab.id, {                     │
│      type: 'ui_chat_history',                                  │
│      data: { chat_id, messages }                               │
│    })                                                           │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 5. Content script populates HUD chat with message history      │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│ ERROR HANDLING                                                  │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ If error occurs on server:                                     │
│ Server → Service Worker:                                       │
│   { type: 'chat_error', data: { error details } }              │
│ Service Worker → Content Script:                               │
│   { type: 'ui_chat_error', data: message }                     │
│ Content script displays error in HUD                           │
└────────────────────────────────────────────────────────────────┘
```

---

### 8. Orb State Persistence

The service worker maintains orb UI state across page navigations and browser restarts.

```
┌────────────────────────────────────────────────────────────────┐
│ STATE PERSISTENCE ACROSS NAVIGATION                            │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 1. User changes orb state (moves orb, changes theme, etc.)     │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 2. Content script → Service Worker                             │
│    chrome.runtime.sendMessage({                                │
│      type: 'set_orb_state',                                    │
│      theme: 'robot',                                           │
│      position: { left: 20, top: 20 },                          │
│      chatVisible: true,                                        │
│      chatInput: 'user typing...',                              │
│      chatPanelSize: { width: 400, height: 600 }                │
│    })                                                           │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 3. Service Worker updates orbState object                      │
│    If chatPanelSize changed:                                   │
│      chrome.storage.local.set({ chatPanelSize })               │
│    (Persists chat panel size for browser restart)              │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 4. User navigates to new page                                  │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 5. New page's content script initializes                       │
│    chrome.runtime.sendMessage({ type: 'get_orb_state' })       │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 6. Service Worker responds with orbState + zoom level          │
│    { ok: true, theme, position, chatVisible, chatInput,        │
│      chatPanelSize, zoom }                                     │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│ 7. Content script restores orb state from response             │
│    - Orb appears at same position                              │
│    - Same theme applied                                        │
│    - Chat panel restored if was visible                        │
│    - Chat input text restored                                  │
│    - Chat panel size restored                                  │
└────────────────────────────────────────────────────────────────┘
```

---

## Message Types Reference

### Messages FROM Server (WebSocket)

| Type | Purpose | Handler |
|------|---------|---------|
| `site_configs_update` | Update site configurations | handleServerMessage → stores + forwards to content scripts |
| `execute_llm_action` | Execute action on page element | handleExecuteLLMAction |
| `execute_capability` | Execute capability command | handleExecuteCapability |
| `server_ping` | Server heartbeat check | handleServerMessage → send pong |
| `toggle_hud` | Toggle HUD overlay on active tab | handleToggleHUD |
| `set_orb_theme` | Change orb theme | handleSetOrbTheme |
| `get_orb_themes` | Get available orb themes | handleGetOrbThemes |
| `chat_append_ack` | Chat message append acknowledgment | handleServerMessage → forward to content script |
| `chat_error` | Chat error from server | handleServerMessage → forward to content script |
| `chat_history` | Chat history response | handleServerMessage → forward to content script |
| Command messages | Various DOM/tab commands | Routed by command type |

---

### Messages TO Server (WebSocket)

| Type | Purpose | Sent By |
|------|---------|---------|
| `bridge_status` | Extension connection status | connectWebSocket |
| `tabs_info` | All tabs information | sendTabsInfo |
| `active_tab_info` | Current active tab info | sendActiveTabInfo |
| `ping` | Extension heartbeat | sendHeartbeat |
| `pong` | Response to server ping | handleServerMessage |
| `intelligence_update` | Page scan results | handleIntelligenceUpdate, handleScanComplete |
| `dom_content_changed` | Significant DOM changes | handleDOMChanged |
| `network_activity` | Network request tracking | handleNetworkActivity |
| `immediate_scan_results_after_tear_away` | Tear away scan results | handleImmediateScanResults |
| `hud_response` | HUD toggle result | handleToggleHUD |
| `orb_theme_response` | Orb theme change result | handleSetOrbTheme |
| `orb_themes_response` | Available orb themes | handleGetOrbThemes |
| `chat_user_message` | User chat message from HUD | ui_chat_user_message handler |
| `get_chat_history` | Request chat history | ui_get_chat_history handler |
| Response messages | Command responses | sendSuccessResponse, sendErrorResponse |

---

### Messages FROM Content Scripts (chrome.runtime.sendMessage)

| Type | Purpose | Handler |
|------|---------|---------|
| `intelligence_update` | Scan complete with data | handleIntelligenceUpdate |
| `scan_complete` | Scan finished | handleScanComplete |
| `request_scan` | Request new scan | requestScan |
| `dom_changed` | DOM mutation detected | handleDOMChanged |
| `network_activity` | Network request event | handleNetworkActivity |
| `get_site_config_for_domain` | Lookup site config | handleGetSiteConfigForDomain |
| `immediate_scan_results` | Tear away scan results | handleImmediateScanResults |
| `get_orb_state` | Request current orb state | get_orb_state handler |
| `set_orb_state` | Update orb state | set_orb_state handler |
| `ui_chat_user_message` | User chat message from HUD | ui_chat_user_message handler |
| `ui_get_chat_history` | Request chat history | ui_get_chat_history handler |
| `ping` | Connection check | Simple pong response |

---

### Messages TO Content Scripts (chrome.tabs.sendMessage)

| Type | Purpose | Sent By |
|------|---------|---------|
| `start_scan` | Initiate DOM scan | requestScan |
| `site_configs_update` | New site configs | handleServerMessage |
| `execute_action` | Execute LLM action | handleExecuteLLMAction |
| `execute_capability` | Execute capability | handleDOMCapability |
| `toggle_hud` | Toggle HUD overlay | handleToggleHUD |
| `set_orb_theme` | Change orb theme | handleSetOrbTheme |
| `get_orb_themes` | Get available themes | handleGetOrbThemes |
| `apply_orb_theme` | Apply theme from popup | set_orb_theme handler |
| `ui_chat_append_ack` | Chat append acknowledgment | chat_append_ack handler |
| `ui_chat_error` | Chat error from server | chat_error handler |
| `ui_chat_history` | Chat history from server | chat_history handler |
| DOM commands | Various DOM operations | handleDOMCommand |

---

### Messages FROM Popup (chrome.runtime.sendMessage)

| Type | Purpose | Handler |
|------|---------|---------|
| `setWsUrl` | Update WebSocket URL | handleSetWsUrl |
| `forceRefresh` | Force refresh all tabs | handleForceRefresh |
| `clearAllCache` | Clear all cached data | handleClearAllCache |
| `getStatus` | Get extension status | handleGetStatus |
| `set_orb_theme` | Set orb theme from popup | set_orb_theme handler |
| `force_content_script_reinjection` | Force content script refresh | handleForceContentScriptReinjection |
| `force_extension_reload` | Force extension reload | handleForceExtensionReload |

---

## Known Issues and Notes

### 1. Undefined Variables/Functions

**actionInProgress**
- Used in: ensureContentScriptFresh(), handleDOMChanged(), handleNetworkActivity(), handleExecuteLLMAction()
- Problem: Variable is referenced but never declared
- Impact: Will cause ReferenceError if used before assignment in handleExecuteLLMAction()
- Fix needed: Declare at top of file with other state variables

**proactivelySendSiteConfig(tabId, url)**
- Called in: ensureContentScriptFresh(), handleDOMCommand()
- Problem: Function is called but not defined anywhere in file
- Impact: Will cause ReferenceError when called
- Fix needed: Define function or remove calls

**getCurrentActiveTabId()**
- Called in: handleForceContentScriptReinjection()
- Problem: Function is called but not defined anywhere in file
- Impact: Will cause ReferenceError when called
- Alternative: Use findActiveTab() instead

---

### 2. Disabled Features

**Page Version System** (lines 38-221)
- Entire system commented out
- Previously tracked page versions per tab+domain
- Replaced with simple sequential IDs in text.md
- Can be safely deleted after testing confirms no issues

**DOM Mutation Rescans** (line 1659-1677)
- Full rescans on DOM mutations disabled
- Reason: Caused counter accumulation, ID inconsistency, overlapping scans
- Now: content.js handles mutations incrementally via registerInteractiveSubtree

**Network Idle Rescans** (line 1741-1754)
- Automatic rescans during action execution disabled
- Reason: Prevents invalidating action IDs before actions complete
- Now: Rescans triggered post-action instead

---

### 3. Deprecated Code

**triggerIntelligenceScan()** (line 636-639)
- Marked as deprecated
- Redirects to requestScan()
- Kept for compatibility
- Should be removed after updating all callers

**tabScanState Map** (line 230)
- Marked as deprecated
- Still used in some listeners for cleanup
- Will be removed in future

---

### 4. Design Notes

**Scan Deduplication**
- Only one scan can run per tab at a time
- Same URL scans are skipped unless forced trigger
- Forced triggers: post_action, significant_dom_change, page_refresh, tab_switch, post_scroll, inputPattern_submit

**Action Coordination**
- actionInProgress flag prevents content script refresh during action execution
- Post-action scan triggered after 1s delay
- This ensures action IDs remain valid during execution

**Tab Accessibility**
- chrome://, about:, chrome-extension:// URLs are not accessible for content scripts
- findActiveTab() implements multiple fallback strategies
- Prioritizes non-chrome tabs over chrome:// tabs

**Keep-Alive Strategy**
- Multiple redundant mechanisms to prevent suspension
- Port connections from content scripts most reliable
- Offscreen document fallback for when no tabs available
- Periodic checks and alarms as additional safety nets

---

## Initialization Sequence

```
Service Worker Loads
        │
        ▼
┌───────────────────────────────────────┐
│ Global scope executes:                │
│ - connectWebSocket()                  │
│ - ensureKeepAlivePort()               │
│ - scheduleHeartbeatAlarm()            │
│ - setInterval for keep-alive check    │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ chrome.runtime.onStartup (if browser │
│ startup) or onInstalled (if install): │
│ - connectWebSocket()                  │
│ - ensureKeepAlivePort()               │
│ - scheduleHeartbeatAlarm()            │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ WebSocket connects:                   │
│ - Send bridge_status                  │
│ - Send tabs_info                      │
│ - Send active_tab_info                │
│ - Flush pending messages              │
│ - Send heartbeat                      │
└───────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────┐
│ Ready to receive commands             │
└───────────────────────────────────────┘
```

---

## Configuration

### Constants

```javascript
// Keep-alive configuration
const KEEP_ALIVE_PORT_NAME = "ome_keep_alive";
const KEEP_ALIVE_CHECK_INTERVAL_MS = 30 * 1000;  // 30 seconds
const HEARTBEAT_ALARM_NAME = "ome_ws_heartbeat";
const HEARTBEAT_PERIOD_MINUTES = 1;

// WebSocket configuration
const WS_URL = "ws://127.0.0.1:17892";  // Hardcoded in connectWebSocket()

// Timing constants (implicit in code)
const CONTENT_SCRIPT_INIT_DELAY = 100;    // ms - wait for content script
const POST_ACTION_SCAN_DELAY = 1000;      // ms - wait before post-action scan
const POST_SCROLL_SCAN_DELAY = 300;       // ms - wait before post-scroll scan
const NETWORK_IDLE_SCAN_DELAY = 1000;     // ms - wait after network idle (disabled)
const TAB_RELOAD_SCRIPT_DELAY = 2000;     // ms - wait before re-inject after reload
```

---

## Best Practices for Modification

1. **Always update this documentation** when changing sw.js
2. **Test WebSocket reconnection** after changes to connection logic
3. **Verify keep-alive** still works after modifying port handling
4. **Check scan deduplication** after changing requestScan() logic
5. **Test with chrome:// tabs** to ensure fallback strategies work
6. **Monitor console** for ReferenceErrors from undefined functions/variables
7. **Test action execution** to ensure actionInProgress coordination works
8. **Verify site config distribution** after changes to config handling

---

## Debugging Tips

### Enable Verbose Logging
Service worker logs to extension background page console:
1. chrome://extensions/
2. Click "service worker" link under Om_E_Web
3. Console shows all [SW] prefixed logs

### Common Issues

**WebSocket not connecting**
- Check ws_server.py is running on port 17892
- Check console for connection errors
- Verify no firewall blocking localhost:17892

**Content script not responding**
- Check tab is accessible (not chrome://)
- Verify content script injected (check tab's console)
- Try force refresh via popup

**Scans not triggering**
- Check scan deduplication logic
- Verify trigger type is in forced list if needed
- Check scanInProgress flag not stuck

**Actions failing**
- Verify actionId exists in content script registry
- Check active tab is correct
- Monitor actionInProgress flag state

---

## Summary

The service worker is the central nervous system of Om_E_Web, orchestrating:
- **Bidirectional communication** between server and content scripts
- **Tab lifecycle management** with intelligent state tracking
- **Scan coordination** with deduplication and trigger routing
- **Keep-alive mechanisms** to prevent Chrome suspension
- **Site configuration distribution** for per-domain behavior
- **Smart capability routing** between browser-level and page-level operations
- **Chat pipeline bridging** between HUD UI and WebSocket server
- **Orb state persistence** across page navigations and browser restarts
- **HUD interface control** for overlay visibility and theme management

It implements a robust, event-driven architecture with multiple fallback strategies for reliability. Key to its operation is the clear separation between:
- **Browser-level operations** (handled directly via Chrome APIs)
- **Page-level operations** (forwarded to content scripts)
- **State synchronization** (maintaining consistency across components)
- **UI state persistence** (orb position, theme, chat visibility, panel size)
